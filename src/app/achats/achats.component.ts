import{Component,inject}from'@angular/core';import{MatSnackBar}from'@angular/material/snack-bar';import{CountryService}from'../core/services/country.service';import{DemoStoreService}from'../core/services/demo-store.service';@Component({templateUrl:'./achats.component.html',styleUrl:'./achats.component.scss'})export class AchatsComponent{store=inject(DemoStoreService);country=inject(CountryService);snack=inject(MatSnackBar);columns=['numero','fournisseur','date','livraison','lignes','total','statut','actions'];receive(id:number){this.store.receiveOrder(id);this.snack.open('Commande réceptionnée, stock mis à jour','Fermer',{duration:3500})}}
