import { Component, inject } from '@angular/core'; import { DemoStoreService } from '../core/services/demo-store.service'; import { CountryService } from '../core/services/country.service'; import{MatSnackBar}from'@angular/material/snack-bar';
@Component({templateUrl:'./historique-ventes.component.html'}) export class HistoriqueVentesComponent { store=inject(DemoStoreService); country=inject(CountryService);snack=inject(MatSnackBar); columns=['numero','date','client','vendeur','articles','paiement','total','statut','actions'];refund(id:number){if(confirm('Confirmer le retour total ? Les pièces seront remises en stock.')){this.store.refundSale(id);this.snack.open('Retour enregistré et stock restauré','Fermer',{duration:3500})}} }
