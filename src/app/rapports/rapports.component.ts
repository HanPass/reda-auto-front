import{Component,inject}from'@angular/core';import{CountryService}from'../core/services/country.service';import{DemoStoreService}from'../core/services/demo-store.service';@Component({templateUrl:'./rapports.component.html',styleUrl:'./rapports.component.scss'})export class RapportsComponent{store=inject(DemoStoreService);country=inject(CountryService);categories=['Freinage','Filtration','Moteur','Refroidissement','Électricité'];values=[92,76,68,55,43];get revenue(){return this.store.sales.reduce((s,v)=>s+v.total,0)}get purchases(){return this.store.orders.reduce((s,o)=>s+o.total,0)}get stock(){return this.store.products.reduce((s,p)=>s+p.prixAchat*p.stock,0)}}
